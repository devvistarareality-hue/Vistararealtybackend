{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python manage.py run_scheduled_notifications",
    "cronSchedule": "*/30 * * * *",
    "restartPolicyType": "NEVER"
  }
}
